[pytest]
testpaths = tests
markers =
    ui: UI tests using Playwright
    api: API tests using requests
    integration: end-to-end API + UI integration tests
    mobile: mobile tests via BrowserStack
